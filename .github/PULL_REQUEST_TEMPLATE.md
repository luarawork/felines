## What does this change?

A short description of what this PR does and why.

## Related issue

Closes #

## Checklist

- [ ] `npm run build` passes with zero errors
- [ ] `npm run lint` passes with zero warnings
- [ ] Tested at 375px width (mobile) — no horizontal overflow, no
      overlapping elements, touch targets at least 44×44px
- [ ] Both Portuguese and English translations updated, if any
      user-facing text changed (`lib/i18n/pt.ts` and `lib/i18n/en.ts`)
- [ ] No new colors introduced outside the existing design system
      palette (`app/globals.css`'s `@theme` tokens)

## Screenshots (if applicable)

Before/after screenshots for any visual change.
